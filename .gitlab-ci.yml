image: docker

variables:
    DOCKER_TLS_CERTDIR: "/certs"

services:
    - docker:dind

before_script:
    - docker info

build:
    before_script:
        - ls -la *
        - ls -la /
        - docker info
    script:
        - ./deploy.sh
    after_script:
        - ls build

default:
  image:
    name: madmanfred/qt-webassembly
    entrypoint: ["/bin/bash", "-l", "-c"]

build:
    script:
      - mkdir build
      - qmake CONFIG+=release -o build
      - make -C build
      - ls build
